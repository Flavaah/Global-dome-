// Admin tool for manual password reset
export async function adminResetPassword(email: string, newPassword: string) {
  // TODO: Find user by email, update password in DB
  // Optionally send notification email
  return { success: true };
}
